{
  "version": 2,

  "build": {
    "env": {
      "API_KEY": "@api_key"
    }
  },

  "builds": [
    {
      "src": "nuxt.config.js",
      "use": "@nuxtjs/now-builder",
      "config": {
        "serverFiles": ["server-middleware/**"]
      }
    }
  ]
}
